spring.application.name=BookingSalon

# Database Configuration
spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:bookingsalon}
spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:bookingsalon}?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:Bach@vt05}
spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:update}
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always
spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:validate}
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Flyway Database Migration
spring.flyway.enabled=${FLYWAY_ENABLED:true}
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=0
spring.flyway.locations=classpath:db/migration

# Resource Server
spring.security.oauth2.resourceserver.jwt.issuer-uri=${KEYCLOAK_SERVER_URL:http://localhost:8081}/realms/${KEYCLOAK_REALM:booking-salon-realm}

# Keycloak Admin Client
keycloak.server-url=${KEYCLOAK_SERVER_URL:http://localhost:8081}
keycloak.realm=${KEYCLOAK_REALM:booking-salon-realm}
keycloak.client-id=${KEYCLOAK_CLIENT_ID:booking-salon-admin}
keycloak.client-secret=${KEYCLOAK_CLIENT_SECRET:Nrys6AchVUk9UNPFGqUFkkLYZxhbURqgAulOFZbcjalgVD9cYpwUV51CzjryrYHjqcZxojqkte4PJ87j0m5Rza}

# Redis
spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}
spring.data.redis.database=0
spring.data.redis.password=${REDIS_PASSWORD:}
spring.data.redis.timeout=2s

# Lettuce connection pool
spring.data.redis.lettuce.pool.max-active=8
spring.data.redis.lettuce.pool.max-idle=8
spring.data.redis.lettuce.pool.min-idle=0
spring.data.redis.lettuce.pool.max-wait=2s

# Enable Redis
app.redis.enabled=true

# Payment
# VNPay Payment Gateway
vnpay.tmn-code=${VNPAY_TMN_CODE:U8WZ0T3L}
vnpay.hash-secret=${VNPAY_HASH_SECRET:UCPHPNWGQPJLWGDIAFMGICJPTDGBKPXL}
vnpay.pay-url=${VNPAY_PAY_URL:https://sandbox.vnpayment.vn/paymentv2/vpcpay.html}
vnpay.return-url=${VNPAY_RETURN_URL:http://localhost:8080/api/v1/payments/webhooks/vnpay/callback}
vnpay.notify-url=${VNPAY_NOTIFY_URL:http://localhost:8080/api/v1/payments/webhook/vnpay}


#Sepay Gateway
# Sepay Gateway
sepay.merchant-id=${SEPAY_MERCHANT_ID:YOUR_MERCHANT_ID}
sepay.secret-key=${SEPAY_SECRET_KEY:VIB}
sepay.api-base-url=${SEPAY_API_BASE_URL:https://pgapi.sepay.vn}
sepay.webhook-api-key=${SEPAY_WEBHOOK_API_KEY:YOUR_API_KEY}
sepay.env=${SEPAY_ENV:sandbox}

# Bank Transfer
payment.bank.bin=${PAYMENT_BANK_BIN:123456}
payment.bank.account=${PAYMENT_BANK_ACCOUNT:0866429205}
payment.webhook.secret=${PAYMENT_WEBHOOK_SECRET:}
payment.expire-minutes=${PAYMENT_EXPIRE_MINUTES:15}

logging.level.demo.bookingsalon.payment = DEBUG
logging.org.springframework.web = INFO
logging.level.demo.bookingsalon.payment=DEBUG
logging.org.springframework.web=INFO

# SMTP Email
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:nguyenbachvt05@gmail.com}
spring.mail.password=${MAIL_PASSWORD:ezimnjlvschkhwri}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true


# Cloudinary
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:bzlhomeu}
cloudinary.api-key=${CLOUDINARY_API_KEY:866635999876245}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:FbbjoMXTZ9Nq0JUExp6XyRSpMWU}

# Multipart file
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB
